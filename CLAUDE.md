# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty scaffolding: it contains only a `README.md` with the project title ("vibe-week3") and no source code, build configuration, or tests yet.

There are no build, lint, or test commands to document because no project has been set up. Once a language/framework is chosen and code is added, this file should be updated with:

- Build, lint, and test commands (including how to run a single test)
- High-level architecture notes once the codebase has enough structure to warrant them

## Language

- Claude Code와의 모든 대화는 한국어로 진행합니다.
- 모든 git 커밋 메시지는 한국어로 작성합니다.
